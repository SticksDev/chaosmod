clientConfig = {
    showLogos = true, -- Show logos on server/client start
}

serverConfig = {
    showLogos = true, -- Show logos on server/client start
    eventsEnabled = false, -- Enable events on server start
}